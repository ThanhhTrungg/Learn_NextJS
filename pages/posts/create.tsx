import * as React from 'react';

interface ICreatePostPageProps {
}

const CreatePostPage: React.FunctionComponent<ICreatePostPageProps> = (props) => {
    return <h1>Create Post Page</h1>;
};

export default CreatePostPage;